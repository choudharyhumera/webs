services:
  site:
    image: node:22-bookworm
    working_dir: /workspace
    network_mode: host
    init: true
    volumes:
      - .:/workspace
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      IS_ALLOY: ${IS_ALLOY:-false}
    command: npm run dev
