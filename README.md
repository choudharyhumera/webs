# webs

A small, dependency-free Node.js site configured to run in Alloy.

## Run with Alloy Compose

```sh
docker compose -f docker-compose.alloy.yaml up
```

The application listens on `http://localhost:3000`. Alloy proxies its preview
from `http://localhost:8080`.
